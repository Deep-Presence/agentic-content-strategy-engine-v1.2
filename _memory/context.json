{
  "sprint": "content-studio-detail-view-rework",
  "sprint_goal": "Rework Content Studio detail view: gap analysis metrics in center/sidebar, brief flow enhancement, card vanish bug fix, TD filesystem→DB disconnect audit.",
  "sprint_status": "COMPLETE — (1) Gap analysis metrics: rich center view for gap_analysis_complete with KPI grid + classification breakdown + competitors. Right sidebar GapAnalysisSection persists through all stages. (2) Brief flow: enhanced BriefContent with exemplar details, AgentSidebar shows brief outline during production. (3) Card vanish fix: orchestrator checks piece count before cleanup. (4) TD filesystem audit: 6 gaps documented in .claude/sprints/pending/td-filesystem-db-disconnect-audit.md (2 critical: pipeline_v13.py + content_v13.py still use TopicDiscoveryStorage). (5) GA animation: replaced step labels with abstract orbits to prevent reverse engineering. tsc clean.",
  "constraints": [
    "Backward compatibility with existing artifacts — all Pydantic fields must have defaults",
    "Raw SDK clients for gap analysis + content engine (no LangChain wrappers)",
    "OpenRouter via openai SDK (AsyncOpenAI/OpenAI with base_url override, max_retries=0)",
    "LangGraph >=1.0 interrupt model: check __interrupt__ in result dict, not GraphInterrupt exception",
    "Double-underscore separator for effective slugs: {company}__{product}",
    "Default-deny middleware — all routes protected unless explicitly whitelisted",
    "core/ cannot import from api/ — shared code goes in core/shared_tools/",
    "DATABASE_URL is REQUIRED — no JSON fallback for TaskStore, AuthService, or Topic Discovery",
    "REDIS_URL is REQUIRED — no in-memory fallback for EventBus or checkpointer",
    "Topic Discovery pipeline is DB-only — TopicDiscoveryStorage no longer used in pipeline hot paths",
    "Pipeline B uses additive per-subdomain writes (db_write_assignments_for_subdomain), NOT delete-all + re-insert-all",
    "Pipeline B expansion uses allow_parallel=True — no Redis slug lock; concurrency via db_claim_subdomain_for_expansion (optimistic DB lock)",
    "Stuck 'expanding' nodes auto-reset after 20 minutes (claim_for_expansion stale threshold)",
    "tree_json set NULL during expansion; rebuilt after batch; db_read_taxonomy falls back to flat-node reconstruction",
    "pipeline_state.json only written when Redis is unavailable (Redis is primary CE state store)",
    "Async Redis for hot paths (pipeline writes), sync Redis for locks + sync readers",
    "Distributed locks are fail-closed when Redis is configured (no split-brain fallback)",
    "CSP strict-dynamic disabled in dev (blocks Next.js HMR)",
    "handleUnauthorized must skip redirect when on auth pages (prevents loop)",
    "useSearchParams() requires Suspense boundary in Next.js 14 App Router",
    "OPENROUTER_API_KEY is REQUIRED for Content Engine, Topic Discovery, Perplexity research, CE-0 Planner, AP-1 Suggester, GA-S8 Report, GA Perplexity Engine",
    "VSG-1 author discovery stays on litellm (Anthropic web_search_20250305 tool not supported by OpenRouter)",
    "Gap analysis engines (Claude/Gemini/OpenAI) stay on native SDKs (web search / grounding / reasoning not supported by OpenRouter)",
    "GA Perplexity engine NOW on OpenRouter (Phase 2) — AsyncPerplexity no longer used"
  ],
  "active_branches": ["integration/front-back-2"],
  "environment_notes": [
    "Python 3.12", "async-first", "VS Code + Claude Code extension",
    "Key deps: LangGraph >=0.2, langsmith >=0.2, litellm >=1.50 (VSG-1 only), openai >=2.0 (OpenRouter + native), sqlalchemy[asyncio] >=2.0.30, structlog >=24.1, redis[hiredis] >=5.2.0, perplexityai >=0.2 (GA engine only)"
  ],
  "current_clients": ["Ramp", "Carta", "Mynd"],
  "key_files_in_flux": [
    "core/orchestration/td_content_orchestrator.py (FIXED — piece count check before Redis cleanup + status update, zero pieces reverts to gap_analysis_complete)",
    "core/content_engine/pipeline_v13.py (STALE — lines 1452-1465 still read TopicDiscoveryStorage filesystem matrix instead of DB — ROOT CAUSE of card vanish bug)",
    "api/routers/content_v13.py (STALE — lines 690-702 topic-content-status endpoint reads filesystem matrix)",
    "frontend/src/app/(dashboard)/content-studio/_components/FullPageView.tsx (REWORKED — gap_analysis_complete rich metrics, enhanced BriefContent, abstract GA animation)",
    "frontend/src/app/(dashboard)/content-studio/_components/RightSidebar.tsx (EXTENDED — GapAnalysisSection in MetricsTab)",
    "frontend/src/app/(dashboard)/content-studio/_components/LeftSidebar.tsx (EXTENDED — brief data in AgentSidebar during production, abstract GA pipeline dots)",
    "frontend/src/app/(dashboard)/content-studio/_hooks/useGapSummary.ts (NEW — fetch gap analysis summary)"
  ],
  "test_count": 1728,
  "phases_completed_count": 70,
  "recent_phases": [
    "content-studio-detail-view-rework — Rich gap analysis metrics (center + sidebar), brief flow enhancement, card vanish bug fix, TD filesystem→DB disconnect audit (6 gaps documented). 8 frontend files, 1 backend fix. tsc clean.",
    "content-performance-gaps — Fill 4 backend gaps (citations, platforms, queries_covered, citation_timeline). 3 repo methods, service enrichment (optional repos), DI wiring, frontend adapters + drawer chart. Codex reviewed. 63 tests passing.",
    "content-performance-frontend-integration — Wire Content Performance page to 3 backend endpoints. Eliminate all mock data. Adapter layer (6 functions). useContentPerformanceData hook (parallel fetch). Drawer real data (daily_traffic, source_breakdown, ai_platform_breakdown). CPS scatter removed. structural_score integrated. tsc + build clean.",
    "content-to-prompt-pipeline — Generate k AI visibility prompts per content inventory page. Join table M:N, ContentToPromptService (OpenRouter Sonnet), orchestrator, 6 API endpoints, enrich_thin_pages(). Codex-reviewed. 42+17 tests. Post-review: 6 security fixes (tenant isolation, prompt injection, dedup race). Structural composite score (0-100, 24 signals). CP page audit: citation-matching service is next priority.",
    "ga-sidebar-enrichment — Enrich GA-phase sidebar with topic assignment metadata (11 fields end-to-end). Eliminate all 6 excluded buyer_stage × intent_type combos (12 cells valid). GapAnalysisSidebar redesigned. 43 tests passing.",
    "td-ga-ce-two-phase-split — Two-phase TD→GA→CE pipeline. Split orchestrator, GA SSE (8 steps), Redis ga_phase_state, 2 new endpoints, frontend GA views + Planner wiring. Codex audit: 7 fixes. 49 new tests. 85 passing.",
    "content-studio-status-enum — Resolve frontend-backend stage/status mismatch. BriefPipelineStatus (17→20 values). status-adapter.ts pure mapping layer. 4-column Kanban.",
    "td-additive-upsert — Refactor Pipeline B to additive per-subdomain DB upserts. 5 new db_ops functions, retry/backoff, circuit breaker, per-assignment persona affinity. Migration 0031. 18 new tests.",
    "td-db-only-migration — Eliminate JSON filesystem from TD pipelines. DB-only storage. Migration 0029. 1588 tests passing."
  ],
  "phases_remaining": [
    "openrouter-phase3 — Embeddings: Point embedding clients at OpenRouter",
    "openrouter-phase4 — Cost tracking for native calls: track_llm_cost() wrapper for ~16 non-migrated sites",
    "openrouter-phase5 (optional) — LangChain adapter: KB Synthesis + Reddit HIL via ChatOpenAI(base_url=openrouter)"
  ],
  "deferred_issues_location": ".claude/sprints/pending/backlog.md"
}
