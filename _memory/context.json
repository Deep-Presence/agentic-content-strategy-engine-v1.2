{
  "sprint": "openrouter-phase2",
  "sprint_goal": "Phase 2 — OpenRouter Migration. Migrate 4 direct SDK calls (Anthropic, Google Gemini, OpenAI responses.create, AsyncPerplexity) to OpenRouter for centralized cost tracking.",
  "sprint_status": "COMPLETE — 7-step plan fully executed. 4 direct SDK call sites migrated, 8 source files + 3 test files modified. 1865 tests passing across all affected modules.",
  "constraints": [
    "Backward compatibility with existing artifacts — all Pydantic fields must have defaults",
    "Raw SDK clients for gap analysis + content engine (no LangChain wrappers)",
    "OpenRouter via openai SDK (AsyncOpenAI/OpenAI with base_url override, max_retries=0)",
    "LangGraph >=1.0 interrupt model: check __interrupt__ in result dict, not GraphInterrupt exception",
    "Double-underscore separator for effective slugs: {company}__{product}",
    "Default-deny middleware — all routes protected unless explicitly whitelisted",
    "core/ cannot import from api/ — shared code goes in core/shared_tools/",
    "DATABASE_URL is REQUIRED — no JSON fallback for TaskStore or AuthService",
    "REDIS_URL is REQUIRED — no in-memory fallback for EventBus or checkpointer",
    "OPENROUTER_API_KEY is REQUIRED for Content Engine, Topic Discovery, Perplexity research, CE-0 Planner, AP-1 Suggester, GA-S8 Report, GA Perplexity Engine",
    "VSG-1 author discovery stays on litellm (Anthropic web_search_20250305 tool not supported by OpenRouter)",
    "Gap analysis engines (Claude/Gemini/OpenAI) stay on native SDKs (web search / grounding / reasoning not supported by OpenRouter)",
    "GA Perplexity engine NOW on OpenRouter (Phase 2) — AsyncPerplexity no longer used"
  ],
  "active_branches": ["feat/openrouter-phase1"],
  "environment_notes": [
    "Python 3.12", "async-first", "VS Code + Claude Code extension",
    "Key deps: LangGraph >=0.2, langsmith >=0.2, litellm >=1.50 (VSG-1 only), openai >=2.0 (OpenRouter + native), sqlalchemy[asyncio] >=2.0.30, structlog >=24.1, redis[hiredis] >=5.2.0, perplexityai >=0.2 (GA engine only)"
  ],
  "current_clients": ["Ramp", "Carta", "Mynd"],
  "key_files_in_flux": [
    "core/shared_tools/openrouter_client.py (+_ensure_model_prefix() moved here from llm_client.py)",
    "core/content_engine/planner.py (Phase 2 — AsyncAnthropic → get_async_client(), response shape converted)",
    "core/research/audience_persona/agents.py (Phase 2 — google.genai → get_async_client(), _get_gemini_api_key() removed)",
    "core/gap_analysis/steps/s8_generate_report.py (Phase 2 — responses.create → chat.completions.create)",
    "core/gap_analysis/engines/perplexity.py (Phase 2 — AsyncPerplexity → get_async_client(), dual citation extraction)",
    "core/gap_analysis/steps/s3_search_platforms.py (Phase 2 — Perplexity client = singleton, no exit-stack)",
    "core/config/settings.py (3 model defaults prefixed: anthropic/claude-opus-4-6, google/gemini-3-flash-preview, openai/gpt-5.2-2025-12-11)"
  ],
  "test_count": 1865,
  "phases_completed_count": 60,
  "recent_phases": [
    "openrouter-phase2 — OpenRouter migration: 4 direct SDK calls (CE-0 planner, AP-1 suggester, GA-S8 report, GA Perplexity engine). 8 source + 3 test files. 1865 tests passing.",
    "openrouter-phase1 — OpenRouter migration: 3 wrappers (llm_call, _run_completion, perplexity_client.research) + VSG-3. ~32 call sites, 18 files, 386 tests passing.",
    "redis-phase6-remove-fallbacks — Delete JSON TaskStore, EventBus, JsonAuthService, MemorySaver. DATABASE_URL+REDIS_URL mandatory. 12 steps, ~1300 lines removed, 1360 tests passing.",
    "r2-blob-storage-phase5 — Reader-side StorageBackend migration: json_site_audit_data full refactor, content/brand/prompt services fixed, DI wiring, 447 tests verified",
    "r2-blob-storage-phase3 — CachedStorageBackend, mtime removal, brand_data fix, list_dir caching (25 new tests)",
    "redis-codex-fixes-phase2 — 6 critical/high bug fixes: singleton race, socket leak, cache mismatch, cleanup clobber, semaphore renewal, exception broadening (17 new tests)"
  ],
  "phases_remaining": [
    "openrouter-phase2 — COMPLETE. Direct SDK calls migrated: CE-0 planner, AP-1 suggester, GA-S8 report, GA Perplexity engine.",
    "openrouter-phase3 — Embeddings: Point embedding clients at OpenRouter",
    "openrouter-phase4 — Cost tracking for native calls: track_llm_cost() wrapper for ~16 non-migrated sites",
    "openrouter-phase5 (optional) — LangChain adapter: KB Synthesis + Reddit HIL via ChatOpenAI(base_url=openrouter)"
  ],
  "deferred_issues_location": ".claude/sprints/pending/backlog.md"
}
