{
  "sprint": "structured-logging-layer2-v18",
  "sprint_goal": "Structured Logging Layer 2 — Correlation & Context Propagation. X-Request-ID middleware, LangSmith trace linkage, SSE context binding (PB-83), auth failure logging (PB-84), per-step/per-agent context binding (PB-85), run_id binding, async context propagation tests.",
  "sprint_status": "COMPLETE — 8 phases, 4 new test files, 12 modified files. 48 new tests. Codex-reviewed (6 findings incorporated). 3 backlog items resolved (PB-83, PB-84, PB-85). Zero regressions.",
  "constraints": [
    "Backward compatibility with existing artifacts — all Pydantic fields must have defaults",
    "Raw SDK clients for gap analysis + content engine (no LangChain wrappers)",
    "LiteLLM wraps all LLM calls in content engine workers/judges/agents",
    "LangGraph >=1.0 interrupt model: check __interrupt__ in result dict, not GraphInterrupt exception",
    "No Supabase yet — filesystem artifacts are source of truth",
    "Double-underscore separator for effective slugs: {company}__{product}",
    "Default-deny middleware — all routes protected unless explicitly whitelisted",
    "core/ cannot import from api/ — shared code goes in core/shared_tools/",
    "DB layer is opt-in via DATABASE_URL — repos flush only, DI session generator owns commit/rollback",
    "AuthServiceProtocol is the contract — all routers use get_auth_service dependency",
    "LangSmith is sole tracing backend — Langfuse fully removed",
    "Hub prompts opt-in via langsmith_use_hub=False — local Python prompts authoritative by default"
  ],
  "active_branches": ["feat/deployment-prep-sprints"],
  "environment_notes": [
    "Python 3.12", "async-first", "VS Code + Claude Code extension",
    "Key deps: LangGraph >=1.0, langsmith >=0.2, litellm >=1.50, sqlalchemy[asyncio] >=2.0.30, structlog >=24.1"
  ],
  "current_clients": ["Ramp", "Carta", "Mynd"],
  "key_files_in_flux": [
    "core/shared_tools/structured_logging.py (MODIFIED — added get_context_value + scoped_bind)",
    "core/shared_tools/tracing.py (MODIFIED — langsmith_trace_id binding)",
    "api/app.py (MODIFIED — X-Request-ID, try/finally, correlation ID sanitization)",
    "api/routers/events.py (MODIFIED — SSE context binding)",
    "api/auth/middleware.py (MODIFIED — auth failure structured logging)",
    "api/tasks/runner.py (MODIFIED — run_id binding in all 14 runner functions)",
    "6 pipeline files (MODIFIED — scoped_bind for step_name/agent_name)"
  ],
  "test_count": 2773,
  "phases_completed_count": 50,
  "recent_phases": [
    "structured-logging-layer2 v18 — 8 phases (48 new tests, 3 backlog items resolved)",
    "structured-logging v17 — Phase A (foundation) + Phase B (context propagation) + Codex audit (22 new tests)",
    "pgvector-migration v16 — 8 phases + Codex audit (493 new tests, 198 deleted)",
    "daily-tracker-db-persistence v15 — 6 phases + Codex audit (392 DT tests, 41 new)",
    "td-content-integration v1.2.0 — 8 phases (44 new tests)"
  ],
  "phases_remaining": [
    "audience-persona Phase E — API Router + Runner (~30 tests)",
    "audience-persona Phase F — Integration (~20 tests)",
    "knowledge-base Phase 6 — Integration (wire to gap analysis, Daily Tracker, frontend)"
  ],
  "deferred_issues_location": ".claude/sprints/pending/backlog.md"
}
