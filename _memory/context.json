{
  "sprint": "r2-blob-storage-phase0-v19",
  "sprint_goal": "R2 Blob Storage Migration Phase 0 — Infrastructure Layer. Create R2StorageBackend, factory switching, binary method support, settings, app startup wiring. After Phase 0, all pipeline writes via StorageBackend auto-write to R2 in production with zero pipeline code changes.",
  "sprint_status": "COMPLETE — 8 steps, 4 new files, 9 modified files. 84 new tests. Codex-reviewed (GPT-5.3-Codex: 9 findings, 5 fixed). Zero regressions on 790 existing research tests.",
  "constraints": [
    "Backward compatibility with existing artifacts — all Pydantic fields must have defaults",
    "Raw SDK clients for gap analysis + content engine (no LangChain wrappers)",
    "LiteLLM wraps all LLM calls in content engine workers/judges/agents",
    "LangGraph >=1.0 interrupt model: check __interrupt__ in result dict, not GraphInterrupt exception",
    "No Supabase yet — filesystem artifacts are source of truth",
    "Double-underscore separator for effective slugs: {company}__{product}",
    "Default-deny middleware — all routes protected unless explicitly whitelisted",
    "core/ cannot import from api/ — shared code goes in core/shared_tools/",
    "DB layer is opt-in via DATABASE_URL — repos flush only, DI session generator owns commit/rollback",
    "AuthServiceProtocol is the contract — all routers use get_auth_service dependency",
    "LangSmith is sole tracing backend — Langfuse fully removed",
    "Hub prompts opt-in via langsmith_use_hub=False — local Python prompts authoritative by default"
  ],
  "active_branches": ["feat/front-back"],
  "environment_notes": [
    "Python 3.12", "async-first", "VS Code + Claude Code extension",
    "Key deps: LangGraph >=1.0, langsmith >=0.2, litellm >=1.50, sqlalchemy[asyncio] >=2.0.30, structlog >=24.1"
  ],
  "current_clients": ["Ramp", "Carta", "Mynd"],
  "key_files_in_flux": [
    "core/storage/backends/r2.py (CREATED — R2StorageBackend, 8 methods, paginated list_dir, path traversal, exists prefix fallback)",
    "core/storage/backends/base.py (MODIFIED — added read_bytes/write_bytes abstract methods)",
    "core/storage/backends/local.py (MODIFIED — binary read/write with atomic writes)",
    "core/storage/__init__.py (MODIFIED — factory switching STORAGE_BACKEND=local|r2, lazy R2 import)",
    "core/config/settings.py (MODIFIED — 6 R2 settings fields + effective_r2_endpoint_url property)",
    "api/app.py (MODIFIED — startup uses get_storage_backend() factory)"
  ],
  "test_count": 2857,
  "phases_completed_count": 51,
  "recent_phases": [
    "r2-blob-storage-phase0 v19 — 8 steps + Codex audit (84 new tests, zero regressions)",
    "structured-logging-layer2 v18 — 8 phases (48 new tests, 3 backlog items resolved)",
    "structured-logging v17 — Phase A (foundation) + Phase B (context propagation) + Codex audit (22 new tests)",
    "pgvector-migration v16 — 8 phases + Codex audit (493 new tests, 198 deleted)",
    "daily-tracker-db-persistence v15 — 6 phases + Codex audit (392 DT tests, 41 new)"
  ],
  "phases_remaining": [
    "R2 Phase 1 — Gap Analysis retrofit (49 bypasses, 7 step files + 3 service/router files)",
    "R2 Phase 2 — Content Engine v1.3 retrofit (23 bypasses)",
    "R2 Phase 3 — Site Audit retrofit (24 bypasses)",
    "R2 Phase 4 — AP/VSG cross-pipeline callers (15 bypasses)",
    "R2 Phase 5 — Cache invalidation (mtime→TTL)",
    "R2 Phases 6-8 — Binary upload, DB fallback removal, cleanup"
  ],
  "deferred_issues_location": ".claude/sprints/pending/backlog.md"
}
